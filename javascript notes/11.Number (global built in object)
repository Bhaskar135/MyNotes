
Number Object:

     Method                    Description
  1) isNaN()            Checks if the value is not a number.
  2) toFixed()          Rounds to specified number of decimal places (returns a string).
  3) toPrecision()      Rounds to total number of places (returns a string).
  4) toExponential()    Returns a string representing the number in exponential notation.

  eg,   <p id="info">Hello javascript</p>
        <script>
            var originalNumber=10.23456;
            var msg='<h2>original number</h2><p>'+originalNumber+'</p>';
            msg+='<h2>3 decimal places</h2><p>'+originalNumber.toFixed(3)+'</p>';
            msg+='<h2>3 digits</h2><p>'+originalNumber.toPrecision(3)+'</p>';
            var el=document.getElementById('info');
            el.innerHTML=msg;
        </script>

